import random
import os
import torch
import numpy as np


# ---WARNING: INCOMPLETE---#
def sample(root, config, save_file_path):
    # select indices/filter clean data according to config
    # create sample listfile (dictionary of files to be read by readers)
    # save to file path
    pass
